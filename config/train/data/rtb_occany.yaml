dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: rtb_occany/RECORDING_ID
  keys_to_load:
    - pixels
    - action
    - proprio
    - depth_maps
    - depth_conf
  keys_to_cache:
    - action
    - proprio
