dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: rtb_vp/RECORDING_ID
  keys_to_load:
    - pixels
    - action
    - proprio
    - lane_masks
    - depth_maps
  keys_to_cache:
    - action
    - proprio
