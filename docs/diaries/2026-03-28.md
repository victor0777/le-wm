# 2026-03-28 작업 일지

## Task 1: RTB → HDF5 변환 파이프라인 구축
- **목표**: rosbag-ingest 출력을 LeWM 학습용 HDF5로 변환
- **시도**: velocity/IMU 센서로 action=[vx,vy,angular_yaw], proprio=[speed,heading_rate,accel_xyz,gyro_xyz] 설계. `scripts/convert_rtb_to_hdf5.py` 구현
- **결과**: 성공. 1 recording (69K frames) 변환 7.8GB, 16분 소요. `swm.data.HDF5Dataset` 호환 확인
- **소요**: ~2시간 (설계 + 구현 + 검증)
- **다음**: 추가 recording 변환

## Task 2: 단일 Recording 학습
- **목표**: RTB 데이터로 LeWM 학습 가능한지 검증
- **시도**: 5 epochs, single A100, Gian-Pankyo recording
- **결과**: loss 5.0 → 0.3 (94% 감소). DDP multi-GPU 데드락 발견 → single GPU로 전환
- **소요**: ~25분 학습 + 디버깅
- **다음**: embedding 분석

## Task 3: Embedding 시각화
- **목표**: 학습된 representation 품질 평가
- **시도**: t-SNE (speed/time/yaw), cosine similarity matrix, NN retrieval
- **결과**: speed clustering 명확, temporal coherence 확인, NN sim=0.93-0.98
- **소요**: ~10분
- **다음**: Codex debate로 전략 수립

## Task 4: Codex Debate → ADR-001
- **목표**: 향후 방향 결정
- **시도**: 3-round constructive debate (scaling, action space, evaluation, application)
- **결과**: confidence 8/10. Primary application=anomaly detection, E0 최우선, scaling curve 전략 확정
- **소요**: ~20분
- **다음**: E0 실행

## Task 5: E0 Motion-Conditioning Ablation
- **목표**: action proxy가 모델에 기여하는지 검증
- **시도**: correct/shuffled/zeroed motion 비교 (500 sequences)
- **결과**: correct vs shuffled +35% MSE 차이. Motion conditioning 유효
- **소요**: ~5분
- **다음**: normalizer fix, multi-recording 학습

## Task 6: Normalizer Fix + Multi-Recording 준비
- **목표**: data leak 수정, 다양한 route 데이터 확보
- **시도**: train.py에서 split→normalize 순서 변경. 4개 추가 recording 병렬 변환
- **결과**: 5개 recording (238K frames, 23GB) 준비 완료. normalizer fix 검증 완료
- **소요**: ~30분
- **다음**: whole-recording holdout 학습

## Task 7: Whole-Recording Holdout 학습
- **목표**: cross-route 일반화 검증
- **시도**: `train_multi.py` 구현. 4 train / 1 holdout (Namyang-Gian). 10 epochs
- **결과**: holdout loss 10.1 → 8.1 (20% 감소). ~2.5시간 소요
- **다음**: holdout에서 E0 + embedding 분석 (3/29 진행)
