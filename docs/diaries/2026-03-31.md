# 2026-03-31

## Task 1: 5-rec OccAny Depth 학습 완료
- **목표**: da8241 포함 5개 recording 전체로 OccAny depth 재학습
- **시도**: `train_vp.py data=rtb_occany +holdout=8014dd`, 15 epochs, 4 train (15,601 seq) + 1 val (1,651 seq)
- **결과**:
  - Depth loss: 1.12 → 0.21 (81% 감소)
  - Pred loss: 0.075 → 0.090
  - SIGReg loss: 54.5 → 65.4 (데이터 다양성 증가로 상승)
  - Checkpoint: `~/.stable_worldmodel/L3_occany_5rec/lewm_L3_occany_depth_5rec_epoch_15_object.ckpt`
- **소요**: ~22분 (21:03~21:25)
- **다음**: E0 평가

## Task 2: E0 Motion Ablation — 5-rec 모델 평가
- **목표**: 5-rec 모델의 motion conditioning 효과 측정
- **시도**: holdout 8014dd에서 500 sequences, correct/shuffled/zeroed 비교
- **결과**:
  - Shuffled gap: **+13.6%** (4-rec +20.1% 대비 감소)
  - Zeroed gap: **+3.3%** (4-rec +3.9% 대비 소폭 감소)
  - 원인: da8241이 train의 49% 차지 → 데이터 불균형
- **소요**: ~5분
- **다음**: 가설 수립 및 실험 계획

## Task 3: 가설 수립 — 공간적 오버랩 + Goal Conditioning 부재
- **목표**: motion gap 감소 원인 분석, 근본적 한계 고찰
- **시도**: 데이터 특성 분석 + E2E 주행과의 비교
- **결과**:
  - H1: 오버랩 데이터 → visual 암기 → motion 약화
  - H2: 오버랩 데이터 → "같은 장소, 다른 action" 학습 기회 (H1 반대)
  - H3: 비오버랩 데이터가 generalizable dynamics에 유리
  - **H4 (핵심)**: Goal/route 정보 없이 action은 "관측값"일 뿐 "의도"가 아님 → motion conditioning의 근본적 한계
  - ADR-007 작성, findings.md 업데이트
- **소요**: ~20분
- **다음**: Experiment A (오버랩 효과 분리) 실행

## Task 4: Experiment A — 오버랩 효과 분리 실험
- **목표**: 공간적 오버랩이 motion conditioning에 미치는 영향 검증 (H1~H4)
- **시도**: train_multi.py에 `exclude_names` 파라미터 추가, 3개 실험 병렬 실행 (GPU 0/1/2, 15 epochs each)
  - A1: Livlab 3 train → Livlab holdout (오버랩→오버랩)
  - A2: da8241 train → Livlab holdout (비오버랩→오버랩)
  - A3: Livlab 4 train → da8241 holdout (오버랩→비오버랩)
- **결과**:
  - A1: shuffled gap **+24.3%**, zeroed +6.6% — **오버랩이 motion을 강화**
  - A2: shuffled gap **+1.4%**, zeroed -5.2% — cross-domain motion 무용
  - A3: shuffled gap **-0.4%**, zeroed -13.4% — cross-domain motion 완전 무용, action이 noise
  - **H2 지지, H4 강하게 지지**. H1, H3 기각.
  - Motion conditioning은 place-specific이며 cross-domain 전이 불가
- **소요**: 학습 ~25분 (A1/A2 병렬), E0 평가 ~5분
- **다음**: C2 실험

## Task 5: C2 — Future Action Hint (Multi-Step Rollout)
- **목표**: 모델이 action 활용 capacity가 있는지 vs context 부족인지 검증
- **시도**: `eval_c2_future_action_hint.py` 구현, A1/A2 모델로 5-step rollout (2.5초), 300 sequences
- **결과**:
  - A1 (오버랩): shuffled gap +21.7%(0.5s) → **+43.8%(2.0s)** — gap 누적 증가!
  - A2 (cross-domain): shuffled gap +0.2% → +3.3%, zeroed -5.8% → -10.7% — action이 noise
  - **H4 완전 확인**: capacity는 충분, context(place knowledge)가 핵심
- **소요**: ~10분 (스크립트 작성 + 실행)
- **다음**: C3 + Codex debate

## Task 6: C3 — Scene Type Stratified Analysis
- **목표**: scene type (straight/curve/accel/stop)별 motion sensitivity 차이 확인
- **시도**: eval_c3_scene_stratified.py, A1 모델, E0 + rollout per scene type
- **결과**:
  - stop: shuffled gap **+73.2%** (전체의 36%) — 정차 예측이 gap을 지배
  - curve: +15.7%, straight: +17.9%, accel/decel: +13.8% — moving scene 간 차이 작음
  - Rollout에서 모든 moving scene이 +35~43%로 수렴
  - **Scene type만으로는 place-specific behavior 설명 불가**
- **소요**: ~15분 (agent 실행)

## Task 7: Codex Debate — E2E Learning Roadmap (ADR-008)
- **목표**: E2E learning, scene understanding, scene labeling 방향 설계
- **시도**: 3라운드 constructive debate (gpt-5.4)
- **결과**:
  - **합의**: Phase 0 (validation) → Phase 1 (corridor proof ∥ token conditioning) → Phase 2 (structured transfer) → Phase 3 (goal-conditioned)
  - Codex 핵심 반론: retrieval baseline 필수, segment ID는 place leakage, planner exploit check
  - Confidence: P0 9/10, Track1 8/10, Track2 5/10, Phase2 4/10, Phase3 3/10
  - ADR-008 작성 완료
- **소요**: ~20분
- **다음**: P0-A (retrieval baseline)

## Task 8: P0-A — Retrieval Baseline (Memorization vs Dynamics)
- **목표**: +43.8% motion gap이 dynamics 학습인지 memorization인지 판별
- **시도**: Visual/Visual+Action/Proprio kNN vs LeWM, 3개 train recording → 8014dd holdout, 500 eval pairs
- **결과**:
  - **LeWM (0.175) vs Visual kNN (0.319): 82% 우위 → DYNAMICS**
  - No-change (0.157)이 kNN보다 나음 — embedding 변화가 작아 "안 변한다"가 좋은 예측
  - kNN에 action 추가 시 악화 (+125%) — retrieval에서 action은 noise
  - LeWM action sensitivity: correct vs shuffled +23.3% 유지
- **소요**: ~10분
- **결론**: LeWM은 memorization을 넘어 dynamics를 학습. Corridor planning 투자 정당화

## Task 9: P0-D — Counterfactual Sanity Check
- **목표**: World model이 action 변경에 물리적으로 plausible한 반응을 보이는지 검증
- **시도**: A1 모델, 200 sequences, 5-step rollout, 5개 조건 (logged/brake/steer_left/steer_right/faster)
- **결과**:
  - **3/4 sanity check 통과**:
    1. Logged가 GT에 가장 가까움 (0.309 vs 0.365~0.384) — PASS
    2. Divergence가 horizon과 증가 (4.97x~9.20x growth) — PASS
    3. Steer left/right 반대 방향 (cosine -0.17) — PASS
    4. Logged rank-1 비율 13.5% < random 20% — FAIL (long-horizon error 누적)
  - Brake 최대 divergence (0.41), faster 최소 (0.056) — 물리적으로 합리적
  - Logged 평균 rank (2.48) > random (3.0) — 전반적으로는 나음
- **소요**: ~10분
- **결론**: short-horizon counterfactual evaluator로 사용 가능. Long-horizon planning에는 uncertainty 고려 필요

## Task 10: P0-B — Pseudo-Maneuver Labeling
- **목표**: IMU/velocity에서 저렴한 maneuver label 도출 (Track 2 input)
- **시도**: 미래 2초 궤적에서 cumulative yaw/speed 변화로 6개 class 분류, 5개 recording 전체
- **결과**:
  - 전체 분포: straight 44.7%, stop 26.1%, decel 9.9%, accel 7.0%, left 6.4%, right 5.9%
  - Temporal consistency: 94.7~98.0% (인접 프레임 일치율)
  - da8241 (고속도로): straight 68%, turn 1.2% — Livlab (시가지): turn 19~24%
  - Label arrays 저장: `~/.stable_worldmodel/rtb_occany_labels/`
  - 시각화: `outputs/p0b_pseudo_maneuvers/` (분포, 예시, consistency)
- **소요**: ~10분 (agent)
- **결론**: Track 2 token conditioning 입력으로 사용 가능, class imbalance (left/right 12%) 주의

## Task 11: Track 1 — Corridor Planning (MPC/CEM)
- **목표**: world model의 planning signal 검증 — CEM으로 action 최적화 가능한지
- **시도**: A1 모델, 100 sequences, 64 candidates, 5-iteration CEM, goal=2.5s ahead embedding
- **결과**:
  - Logged action rank: mean 29.9/64 (top 47%) — median 이상이지만 top-1은 0%
  - **CEM 100% sequences에서 logged보다 나은 action 발견**, 24.8% MSE 감소
  - CEM 수렴 monotonic: 0.375 → 0.282 (smooth cost landscape)
  - **PARTIAL pass**: planning signal 존재, 하지만 embedding MSE ≠ driving quality
- **소요**: ~10분 (agent)
- **다음**: VP/OccAny 기반 cost function 또는 planning fine-tuning

## Task 12: Track 2 — Maneuver Token Conditioning
- **목표**: maneuver token (6-class)이 cross-domain motion gap을 개선하는지 검증
- **시도**: train_vp_token.py 구현 (agent), nn.Embedding(6, embed_dim) added to action embedding, 3 variants
- **결과**:
  - **V3 (cross-domain reverse): -0.4% → +16.7%** — decision gate (+10%) 초과!
  - V2 (cross-domain): +1.4% → +4.0% (소폭 개선)
  - V1 (overlap): +24.3% → +3.0% (하락, epoch 7/15 불완전 학습)
  - 학습이 중간에 Killed됨 (메모리?), 3개 sequential로 재실행
- **소요**: ~30분 (학습 + 평가)
- **결론**: **maneuver token이 cross-domain motion conditioning의 bridge**. Phase 2 진행 정당화.
  V1 하락은 추가 조사 필요 (full 15 epoch 재학습)

## Task 13: V1 Full 15-Epoch Retraining — Regression 해결
- **목표**: V1 overlap regression (+24.3% → +3.0%) 원인 규명
- **시도**: full 15 epoch 재학습 + epoch 5/10/15에서 E0 평가
- **결과**:
  - ep5: +8.3%, ep10: +19.4%, **ep15: +25.8%**
  - A1 baseline +24.3%을 **초과** — maneuver token이 overlap을 해치지 않음
  - Val pred_loss: 0.44(ep1) → 0.21(ep7) → 0.16(ep15) — 아직 수렴 중이었음
- **결론**: **regression은 불완전 학습이 원인**. Maneuver token은 overlap(+25.8%) + cross-domain(+16.7%) 모두에서 유효

## Task 14: Phase 7.2 — Topology Token Labeling + Dual-Token Training
- **목표**: Maneuver token 위에 topology token (curvature x speed_zone x dynamics = 60 classes)을 추가하여 cross-domain motion gap 추가 개선
- **시도**:
  1. `scripts/label_topology_tokens.py`: 5개 recording에서 topology token 도출
     - curvature: straight/gentle/sharp (yaw_rate 1s rolling window)
     - speed_zone: stopped/slow/medium/fast
     - dynamics: cruising/accelerating/braking/stopped/starting
     - Product encoding: 3x4x5 = 60 classes
  2. Token 분포 분석: 36/60 unique tokens, 24 shared across all recordings
     - da8241 (highway): straight/fast/cruising 33.2% 지배
     - Livlab (urban): stopped/stopped 21~46%, 다양한 분포
  3. `train_vp_token_v2.py`: maneuver(6) + topology(60) dual embedding, additive
  4. V3-topo: Livlab 4 train -> da8241 holdout, 15 epochs, CUDA:2
  5. E0 평가 (epoch 10, 15)
- **결과** (E0 shuffled gap, epoch 10 기준):
  - V3 no-token (L2_vp_v3): **+11.9%** (zeroed +5.9%)
  - V3 maneuver-only (track2_v3): **+6.3%** (zeroed -9.6%)
  - V3 maneuver+topology (track2_v3_topo): **+2.5%** (zeroed -6.5%)
  - V3 maneuver+topology epoch 15: **+3.4%** (zeroed -7.4%)
  - **REGRESSION**: topology token 추가가 motion conditioning을 악화
- **소요**: 학습 ~30분, 평가 ~10분, 총 ~50분
- **분석**:
  - speed_zone과 dynamics_state는 action (vx, vy, yaw_rate)에서 직접 파생 → redundancy
  - 3개 embedding additive 합산이 optimization을 어렵게 만듦
  - No-token이 가장 높은 shuffled gap — token이 action shortcut을 만들 가능성
- **다음**: (1) curvature-only token (action과 약하게 상관), (2) VP lane mask 기반 visual topology (action 비의존), (3) cross-attention conditioning

## Task 15: Codex Debate — Cross-Domain Data Requirements (ADR-010)
- **목표**: cross-domain이 현실적 목표인지, 얼마나 많은 데이터가 필요한지 평가
- **시도**: Codex 2라운드 debate + 73개 recording 지역 분포 분석
- **결과**:
  - Codex: 0.5-1M frames for first signal, 최소 6개 독립 지역 필요
  - 73개 recording → 실질 4-5개 독립 지역 (판교, 기안↔판교, Livlab, 기타)
  - GAIA-1은 420M images, DriveDreamer은 ~1M + HD map → 우리와 차원이 다름
  - **결론**: cross-domain은 현재 데이터로 불가, route-specific application 우선
- **소요**: ~15분
- **다음**: route anomaly detection prototype 구축

## Task 16: Route Anomaly Detection Prototype
- **목표**: route-specific surprise score 기반 anomaly detection — 학습된 route의 "정상"을 알고, 예측 오차로 이상 탐지
(이전 diary entry에 결과 기록됨)

## Task 17: 사고 영상 Surprise 분석
- **목표**: 사고 dashcam 영상에서 사고 전 surprise 상승 여부 확인
- **결과**: 약한 signal (ratio 1.51x), pre-crash rise 50-57% (random 수준). Domain gap이 지배적 — 다른 카메라/환경이라 전체가 "surprising"

## Task 18: Auto-Labeling Benchmark
- **목표**: L1/L3/A1 encoder 비교 (NN 기반 road type 분류)
- **결과**: A1 66.0% > L3 60.3% > L1 58.7%. Motion-conditioned encoder가 최고

## Task 19: ROC/Threshold 분석
- **목표**: anomaly detection threshold 최적화
- **결과**: Cosine AUC=0.749 > MSE AUC=0.680. Warning time ≈0 (spike가 갑작스러움)
- **시도**: `scripts/anomaly_detection.py` 구현, A1 모델(epoch 15), sliding window (history=3, predict 1/3/5 steps), holdout(8014dd) vs train(736fcb) 비교
- **결과**:
  - **Holdout vs Train surprise (MSE)**:
    - h=1: 0.178 vs 0.113 (+58.3%, p=3.68e-22)
    - h=3: 0.315 vs 0.225 (+40.4%, p=1.54e-22)
    - h=5: 0.416 vs 0.320 (+30.0%, p=5.00e-23)
  - **Cosine surprise**: holdout/train ratio 1.88~1.99x — MSE보다 강한 신호
  - **Maneuver stratification (h=1)**:
    - stop이 가장 낮음 (0.083 holdout, 0.039 train) — 예측 쉬움
    - accel이 가장 높음 (0.289) — 가속 구간이 가장 "surprising"
    - right turn (0.260) > left turn (0.210) > straight (0.247)
  - **Temporal clustering**: p95 threshold 기준 14개 cluster, 대부분 isolated (길이 1~3)
  - **Top-5 surprise moments**: frame 595 (MSE=1.41), 1210 (1.33), 1200 (1.24), 720 (0.97), 610 (0.87)
    - 시각적으로: 교차로 진입, 좌회전, 갑작스러운 장면 변화 등
  - 시각화 5개 생성: surprise_timeline, surprise_distribution, surprise_by_maneuver, top_surprises, horizon_comparison
- **소요**: ~5분 (스크립트 작성 + 실행)
- **결론**: **surprise metric이 유효**. Holdout이 Train보다 통계적으로 유의하게 높은 surprise (모든 horizon에서 p<1e-20). Maneuver별 차이도 의미 있음 (stop < moving). Route-specific anomaly detection의 기반 확보.
